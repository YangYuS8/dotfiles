registry=https://registry.npmjs.org/
fund=false

# Do not reference the npm token environment variable here. npm and pnpm fail
# while reading this file when the variable is unset. Use npm-auth/pnpm-auth for
# commands that need a token; those helpers create a temporary runtime npmrc
# outside the repository.
